### DevSmith Playwright environment example
# Copy to .env.playwright and fill in values. NEVER commit real credentials.

# Base URL for tests
BASE_URL=http://localhost:3000

# Optional: logging API used by tests
LOGGING_API=http://localhost:3000/api/logs

# GitHub test account (used only for local E2E; leave blank in CI)
GITHUB_TEST_USERNAME=
GITHUB_TEST_PASSWORD=
GITHUB_TEST_OTP=

# Other test flags
PLAYWRIGHT_BASIC_AUTH=
PLAYWRIGHT_BASE_URL=${BASE_URL}

# Note: Keep this file out of git. Add secrets to your local .env.playwright only.

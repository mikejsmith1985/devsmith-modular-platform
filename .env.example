# DevSmith Platform Environment Variables
# Copy this file to .env and fill in your actual values
# See DEPLOYMENT.md for complete setup instructions

# ==========================================
# REQUIRED CONFIGURATION
# ==========================================

# JWT Secret (REQUIRED)
# Generate a secure random string for signing JWT tokens
# Linux/macOS: openssl rand -base64 32
# Windows PowerShell: [Convert]::ToBase64String([byte[]](1..32 | ForEach-Object { Get-Random -Maximum 256 }))
JWT_SECRET=your-secret-key-here-change-me-in-production

# GitHub OAuth (REQUIRED for user authentication)
# Register OAuth app at: https://github.com/settings/developers
# Callback URL must be: http://localhost:3000/auth/github/callback (or your domain)
GITHUB_CLIENT_ID=your_github_oauth_client_id
GITHUB_CLIENT_SECRET=your_github_oauth_client_secret
REDIRECT_URI=http://localhost:3000/auth/github/callback

# ==========================================
# DATABASE CONFIGURATION
# ==========================================

# PostgreSQL Database
POSTGRES_USER=devsmith
POSTGRES_PASSWORD=devsmith
POSTGRES_DB=devsmith
DATABASE_URL=postgresql://devsmith:devsmith@postgres:5432/devsmith

# Redis Cache
REDIS_URL=redis:6379

# ==========================================
# SERVICE PORTS (Docker Internal)
# ==========================================

# External gateway port (accessible from host)
PORT=3000

# Internal service ports (Docker network only)
PORTAL_PORT=3001
REVIEW_PORT=8081
LOGS_PORT=8082
ANALYTICS_PORT=8083

# ==========================================
# AI CONFIGURATION
# ==========================================

# Ollama Configuration (Local AI)
# Endpoint: Where Ollama is running
#   - Local: http://host.docker.internal:11434 (default)
#   - Remote: http://your-ollama-server:11434
OLLAMA_ENDPOINT=http://host.docker.internal:11434

# Default AI Model
# Recommended models:
#   - qwen2.5-coder:7b (12-16GB RAM) - Fast, good quality
#   - deepseek-coder-v2:16b (20-32GB RAM) - Better quality
#   - llama3.1:70b (40GB+ RAM) - Best quality
OLLAMA_MODEL=qwen2.5-coder:7b

# ==========================================
# LOGGING & MONITORING
# ==========================================

# Log Level: debug, info, warn, error
LOG_LEVEL=info

# Logs Service URL (for cross-service logging)
LOG_SERVICE_URL=http://logs:8082/api/logs

# Strict logging mode (fail if logs service unavailable)
LOGS_STRICT=false

# ==========================================
# OPTIONAL CONFIGURATION
# ==========================================

# Environment
NODE_ENV=development
GO_ENV=development

# Frontend Build Timestamp (auto-generated, no need to set)
# BUILD_TIMESTAMP=

# Jaeger Tracing (enabled by default)
# JAEGER_AGENT_HOST=jaeger
# JAEGER_AGENT_PORT=6831

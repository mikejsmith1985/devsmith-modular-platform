import { test, expect, chromium } from '@playwright/test';

// E2E test: Portal login and dashboard user flow
// This test simulates a real user logging in via GitHub OAuth and viewing the dashboard

test.skip('User can login and see dashboard with username/avatar', async () => {
  // This test is flagged as manual-only for now.
});

test.skip('User can login and see dashboard with valid JWT token', async () => {
  // This test is flagged as manual-only for now.
});

### DevSmith integration test environment example
# Copy to .env.test and fill in values for local integration tests.

# Database connection for tests
DATABASE_URL=postgresql://devsmith:password@localhost:5432/devsmith_test

# Review service and other service endpoints
REVIEW_API=http://localhost:8001
LOGS_API=http://localhost:8002

# Test-specific credentials (use CI secrets where appropriate)
TEST_USER_EMAIL=
TEST_USER_PASSWORD=

# Note: Do not commit real secrets. Keep .env.test local and out of git.
